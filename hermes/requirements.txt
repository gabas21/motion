# Motion MCP Server Dependencies
# Install: pip install -r requirements.txt

# FastMCP — Framework untuk membuat MCP Server dengan cepat
# Docs: https://github.com/jlowin/fastmcp
fastmcp>=2.0.0

# httpx — Async HTTP client untuk call ke Motion Backend API
httpx>=0.27.0

# pyyaml — Untuk membaca config.yaml jika diperlukan
pyyaml>=6.0.1
